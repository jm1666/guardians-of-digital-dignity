---
- name: Seal the Mobile Fortress
  hosts: localhost
  connection: local
  become: true

  tasks:
    - name: Ensure Tailscale is active before locking eth0
      ansible.builtin.command: tailscale status --peers=false
      register: ts_status
      changed_when: false
      failed_when: ts_status.rc != 0
      # If this fails, the playbook stops here to prevent a lockout.
      
    - name: Remove the temporary bootstrapper and service file
      ansible.builtin.file:
        path: "{{ item }}"
        state: absent
      loop:
        - "/usr/local/bin/fortress-init.sh"
        - "/etc/profile.d/fortress-nag.sh"
        - "/var/lib/system-provisioned.flag"

    - name: Reload systemd
      ansible.builtin.systemd:
        daemon_reload: true

    - name: Generate unique high-entropy sudo password
      ansible.builtin.set_fact:
        new_sudo_pw: "{{ lookup('ansible.builtin.password', '/dev/null chars=ascii_letters,digits,symbols length=20') }}"

    - name: Apply new password to fortress user
      ansible.builtin.user:
        name: fortress
        password: "{{ new_sudo_pw | password_hash('sha512') }}"
        update_password: always

    - name: Show new password to fortress user
      ansible.builtin.debug:
        msg: 
          - "################################################"
          - "FORTRESS SEALED. NEW SUDO PASSWORD FOR 'fortress':"
          - "{{ new_sudo_pw }}"
          - "################################################"
          - "SUDO NOW REQUIRES THIS PASSWORD. SSH REMAINS KEY-ONLY."

    - name: Confirm new password is saved
      ansible.builtin.pause:
          prompt: "Have you got the password sorted? Press ENTER to disable Bypass Mode (Sudoers)..."

    - name: Remove NOPASSWD entry from cloud-init files
      ansible.builtin.file:
        path: "{{ item }}"
        state: absent
      loop:
        - /etc/sudoers.d/90-cloud-init-users
        - /etc/sudoers.d/010_pi-nopasswd

    - name: Enable Standard Sudo (Password Required)
      ansible.builtin.copy:
        dest: /etc/sudoers.d/fortress-hardened
        mode: '0440'
        content: "fortress ALL=(ALL:ALL) ALL"
        validate: '/usr/sbin/visudo -cf %s'

    - name: Move eth0 from Trusted to Public Zone (Config)
      ansible.builtin.ini_file:
        path: "/etc/NetworkManager/system-connections/{{ item.conn }}.nmconnection"
        section: connection
        option: zone
        value: "{{ item.zone }}"
        mode: '0600'
        no_extra_spaces: true
      loop:
        - { conn: 'eth0', zone: 'public' }
      notify: Reload NetworkManager

    - name: Move eth0 from Trusted to Public Zone (FirewallD)
      ansible.builtin.shell:
        cmd: |
          firewall-cmd --permanent --change-interface=eth0 --zone=public
          firewall-cmd --reload
      register: shred_result
      failed_when: false
      
    - name: Securely shred the traces
      ansible.builtin.shell:
        cmd: |
          # Shred the recovery file
          [ -f /home/fortress/FORTRESS_RECOVERY.txt ] && shred -u /home/fortress/FORTRESS_RECOVERY.txt
          
          # Shred Bash and Root history
          shred -u /home/fortress/.bash_history
          shred -u /root/.bash_history
          
          # Clear current session history
          history -c
      register: shred_result
      failed_when: false # Don't crash if it was already deleted

    - name: Disable SSH Password Authentication
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        regexp: '^#?PasswordAuthentication'
        line: 'PasswordAuthentication no'
      notify: Restart SSH
      
    - name: Final Cleanup
      ansible.builtin.file:
        path: "{{ item }}"
        state: absent
      loop:
        - "/root/.ansible"
        - "/home/fortress/.ansible"
      ignore_errors: true  

  handlers:
    - name: Reload NetworkManager
      ansible.builtin.shell: |
        nmcli general reload
        nmcli connection reload
        nmcli connection up "{{ item.conn }}"
    - name: Restart SSH
      ansible.builtin.service:
        name: ssh
        state: restarted
